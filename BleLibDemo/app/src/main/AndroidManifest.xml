<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cuiqiang.ling.ai.blelibdemo">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".device.DeviceSearchActivity"></activity>

        <activity android:name=".clientTest.ClientBleActivity"></activity>
        <activity android:name=".clientTest.ClientSearchActivity"></activity>
        <activity android:name=".blemusic.BleAvrcpAct"></activity>
        <activity android:name=".client.BleClientActivity"></activity>
        <activity android:name=".client.BleClientSearchActivity"></activity>
        <activity android:name=".device2.BleDeviceSearchActivity"></activity>
    </application>

</manifest>